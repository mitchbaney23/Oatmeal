{
  "name": "@oatmeal/transcribe",
  "version": "1.0.0",
  "description": "Transcription service with whisper.cpp and cloud fallback",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "test": "vitest",
    "lint": "eslint src --ext ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "eventemitter3": "^5.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^7.2.0",
    "typescript": "^5.1.0",
    "vitest": "^0.34.0"
  }
}